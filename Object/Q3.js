var student = {
    name: "David Rayy",
    sclass: "VI",
    rollno: 12
};

// Get the number of properties in the object
var length = Object.keys(student).length;

// Print the length
console.log("Number of properties in the student object:", length);
